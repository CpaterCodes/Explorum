Posts! (WIP) <br><br>
<%= link_to "Generate Log", new_post_path, method: :get %>

<% @posts.reverse.each do |post| %>
  <div class="post_body">
    <%= post.format_date(post.created_at) %>, <%= post.format_time(post.created_at) %><br>
    <%= post.content %>
  </div>
<% end %>

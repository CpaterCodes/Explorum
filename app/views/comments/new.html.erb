
<%= form_with model: [@post, @post.comments.build] do |form| %>
  <%= form.label(:message, "Message to be entered:") %>
  <%= form.text_area :message %> <br>
  <%= form.hidden_field :user_id, value: current_user.id %>
  <%= form.submit "Enter Comment"%>
<% end %>
